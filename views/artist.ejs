<%- include("header", {title: "Artist" })%>
<nav>
    <div class="pages">
        <button onclick="location.href='/playlists';">Playlists</button>
        <button onclick="location.href='/artists';">Artists</button>
    </div>
    <button onclick="location.href='/logout'">Logout</button>
</nav>
<div class="songs">
    <div class="label">
        <h1><%= artist.name %></h1>
        <h1><%= artist.ml %></h1>
    </div>
    <table>
        <tr>
            <th>Song name</th>
            <th>Genre</th>
            <th colspan="2">Actions</th>
        </tr>
        <tbody id="playlist_table">
            <% songs.forEach(song => { %>
                <tr>
                    <td><%= song.name; %></td>
                    <td><%= song.genre; %></td>
                    <td><a href="/edit/song/<%= song._id %>/artist/<%= artist._id %>">Edit</a></td>
                    <td><a href="/delete/song/<%= song._id %>/artist/<%= artist._id %>">Delete</a></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <button onclick="location.href='/new/song/artist/<%= artist._id %>'" class="new">
        New
    </button>
</div>
<%- include("footer")%>
