<%- include("header", {title: "Playlists" })%>
<nav>
    <div class="pages">
        <button class="active">Playlists</button>
        <button onclick="location.href='/artists';">Artists</button>
    </div>
</nav>
<div class="playlists">
    <table>
        <tr>
            <th>Playlist name</th>
            <th>Created by</th>
            <th>Date created</th>
            <th colspan="2">Actions</th>
        </tr>
        <tbody id="playlist_table">
            <% playlists.forEach(playlist => { %>
                <tr>
                    <td><a href="/playlist/<%= playlist.name; %>"><%= playlist.name; %></a></td>
                    <td><%= playlist.creator; %></td>
                    <td><%= playlist.date; %></td>
                    <td><a href="/edit/playlist/<%= playlist.name %>">Edit</a></td>
                    <td><a href="#">Delete</a></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <button onclick="location.href='/new/playlist'" class="new">
        New
    </button>
</div>
<%- include("footer")%>
