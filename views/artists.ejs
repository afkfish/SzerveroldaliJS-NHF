<%- include("header", {title: "Artists" })%>
<nav>
    <div class="pages">
        <button onclick="location.href='/playlists';">Playlists</button>
        <button class="active">Artists</button>
    </div>
</nav>
<div class="artists">
    <table>
        <tr>
            <th>Artist name</th>
            <th>Monthly listeners</th>
            <th colspan="2">Actions</th>
        </tr>
        <tbody id="artist_table">
            <% artists.forEach(artist => { %>
                <tr>
                    <td><a href="/artist/<%= artist.name; %>"><%= artist.name; %></a></td>
                    <td><%= artist.ml; %></td>
                    <td><a href="/edit/artist/<%= artist.name %>">Edit</a></td>
                    <td><a href="#">Delete</a></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <button onclick="location.href='/new/artist'" class="new">
        New
    </button>
</div>
<%- include("footer") %>
